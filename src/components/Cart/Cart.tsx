import React from 'react';
import Navbar from "../Navbar/Navbar";

const Cart: React.FC = () => {
    return (
        <div id="cartContainer">
            <Navbar />
        </div>
    );
}

export default Cart;